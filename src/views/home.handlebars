<main>
    <h1>Home</h1>

    {{#if isAdmin}}

    <div class="home-container">
        {{#each mostrarLista}}
        <ul>
            <li>
                <h4>{{this.title}}</h4>
            </li>
            <li><img src="{{file}}" alt="{{this.title}}"></li>
            <li>{{this.description}}</li>
            <li><strong>${{this.price}}</strong></li>
        </ul>
        {{/each}}
    </div>

    {{else}}
    <h2>No tienes acceso para ver los productos</h2>
    {{/if}}

</main>



<script src="/static/assets/js/index.js"></script>